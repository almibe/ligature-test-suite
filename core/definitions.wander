test-group(
    "definitions tests"
    expect-equal( 
        definitions ()
        definitions ())

    expect-equal( 
        definitions (implies(A B) equivalent(C D))
        definitions (implies(A B) equivalent(C D))))

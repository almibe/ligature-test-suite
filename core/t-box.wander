test-group(
    "t-box tests"
    expect-equal( 
        t-box ()
        t-box ())

    expect-equal( 
        t-box (A )
        t-box (A ))
        

    expect-equal( 
        t-box (A B C)
        t-box (A B C))

    expect-equal( 
        t-box (implies (A B) or (C D))
        t-box (implies (A B) or (C D))))

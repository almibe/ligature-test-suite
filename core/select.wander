test-group(
    "select tests"
    expect-equal(
        "empty select"
        select(
            definitions()
            assertions()
            top())
        set())
    expect-equal(
        "simple select"
        select(
            definitions()
            assertions(triple(a b c))
            top())
        set(
            assertions(triple(a b c))
            assertions())))

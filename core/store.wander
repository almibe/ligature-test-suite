(assert-equal
  (stores)
  [])

(add-store test)

(assert-equal
  (stores)
  [test])

(add-store test2)
(add-store test3)
(remove-store test2)

(assert-equal
  (stores)
  [test test3])

(assert-store test (assertions [a : A]))

-- read-store test
-- | filter (pattern [?name : B])
-- | assert-equal (assertions [a : B]),

-- read-assert-store test
-- | assert-equal (assertions [a : A]),

-- unassert-store test (assertions [a : A]),

-- read-store test
-- | assert-equal (assertions),

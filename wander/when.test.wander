-- Assert.equal "Basic when"
--   (when {} (
--     {} => 1
--   ))

Assert.equal "Basic when" 
  (when (
    false          => 6,
    Bool.not true  => 7,
    Bool.not false => 8,
    _              => 9))
  8

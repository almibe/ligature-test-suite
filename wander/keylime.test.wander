-- instance = Keylime.open "hello",
-- Assert.equal "New instance should be empty" (instance.all {}) [],
-- instance = instance.add 0xFF 0xEE,
-- Assert.equal "New instance should have single pair" (instance.all {}) [[0xFF, 0xEE]],
-- Assert.equal "Read value that exists" (instance.get 0xFF) 0xEE,
-- Assert.equal "Read value that does not exist" (instance.get 0xFE) {},
-- instance = instance.remove 0xFF,
-- Assert.equal "New instance should be empty" (instance.all {}) [],

-- instance = Keylime.new [[0xDDDD, 0xEE], [0xDDEE, 0xFF], [0xFFFF, 0x00]],
-- Assert.equal "New should allow key value pairs" (instance.all {}) [[0xDDDD, 0xEE], [0xDDEE, 0xFF], [0xFFFF, 0x00]],
-- Assert.equal "New should allow key value pairs" (instance.prefix 0xDD) [[0xDDDD, 0xEE], [0xDDEE, 0xFF]],

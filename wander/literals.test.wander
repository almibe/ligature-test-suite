Assert.equal "Int literal" 5 5,
Assert.equal "Bool literal" true true,
Assert.equal "String literal" "Hello" "Hello",
Assert.equal "Lambda literal" (\x -> x) (\x -> x),
Assert.equal "Identifier literal" `hello` `hello`,
Assert.equal "Statement literal" (`hello` `world` 123) (`hello` `world` 123),
Assert.equal "Bytes literal" 0xDEADCAFE 0xDEADCAFE,
Assert.equal "Dataset literal" { `a` `b` `c` } { `a` `b` `c` },

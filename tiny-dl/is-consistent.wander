-- (assert-equal (assertions [a b c]) (infer (define) (assertions [a b c])))

-- (assert-equal
--     (assertions [betty : Cat] [betty : Animal])
--     (infer (definitions (implies Cat Animal)) (assertions [betty : Cat])))

(assert-equal
    true 
    (is-consistent 
        (definitions)
        (assertions [betty : Cat])))

(assert-equal
    false
    (is-consistent 
        (definitions)
        (assertions [betty : Cat] [betty ~ Cat])))

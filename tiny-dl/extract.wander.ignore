(expect-equal
    { @ betty : Cat }
    (extract betty (assertions [betty : Cat])))

-- expect-equal
--     { @ betty : Cat name "Betty" }
--     (extract betty (assertions [betty : Cat] [betty name "Betty"] [bob : Dog])),
